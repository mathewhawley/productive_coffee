%input{id: 'establishment-id', value: "#{@establishment.id}", type: 'hidden'}
%h1= @establishment.name
%h3= @establishment.description
%h3= @establishment.address
%h3= @establishment.phone_number
%h3= @establishment.email
- unless @establishment.reviews.empty?
  #overall-ratings
    %h2= "Overall: #{@overall_rating}"
    %h2= "Power: #{@power_rating}"
    %h2= "Wifi: #{@wifi_rating}"
    %h2= "Coffee: #{@coffee_rating}"
    %h2= "Food: #{@food_rating}"
    %h2= "Service: #{@service_rating}"
    %h2= "Noise Level: #{@noise_level_rating}"

  %h2 Reviews
  %ul.reviews
    - @establishment.reviews.each do |review|
      %div{id: "review-#{review.id}"}
        %li= review.user.first_name
        %p= review.updated_at.to_formatted_s(:long)
        %p= review.content
        %p= review.overall_rating
        - if can? :delete, review
        %button{id: "delete-review", value: "#{review.id}"}= link_to('Delete', establishment_review_path(@establishment, review.id), method: :delete)
- else
  %h2 No Reviews Yet


-# %h2= link_to(@establishment.user.first_name, user_path(@establishment.user.id))

 





  
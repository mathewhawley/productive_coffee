// $(document).ready(function(){
//   var places, userPlace, $input = $("#place");
//   var autocomplete = new google.maps.places.SearchBox($input[0]);
   
//   google.maps.event.addListener(autocomplete, 'places_changed', function() {
//    var places = autocomplete.getPlaces();
//    if (places.length == 0)
//     return;
//    firstPlace = places[0];
//    $.each(places, function(index, place){
//      if (place.formatted_address.indexOf("London") >= 0) {
//         $('<h3>' + place.name + '</h3>' + '<p>' + place.formatted_address + '</p>' + '<img src="' + place.icon + '">').appendTo('#results');
//      }
//    });
   
   
//   });
// });

  